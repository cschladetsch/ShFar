.TH FAR 1 "2026-05-22" "1.0.0" "User Commands"
.SH NAME
far \- find and replace text across files matching a filename glob
.SH SYNOPSIS
.B far
[\fIOPTIONS\fR] \fIroot\fR \fIglob\fR \fIfind\fR \fIreplace\fR
.SH DESCRIPTION
.B far
searches recursively under a root directory for files whose names match a
given glob pattern and whose contents contain a search term, then performs
an in-place substitution replacing all occurrences of the search term with
the replacement string.
.PP
.B far
uses
.BR fd (1)
to locate files by name glob and
.BR rg (1)
to filter by content, then bundled
.BR fsed (1)
\&
.RI ( fastsed )
to perform the substitution.
.SH ARGUMENTS
.TP
.B root
The directory to search under. Use \fI.\fR for the current directory.
.TP
.B glob
A filename glob pattern used to restrict which files are searched.
Examples: \fI*.cpp\fR, \fI*.{h,cpp}\fR, \fIMakefile\fR.
.TP
.B find
The text or basic regular expression to search for within each file.
.TP
.B replace
The replacement string. Backreferences such as \fI\\1\fR are supported.
.SH OPTIONS
.TP
.B \-y
Skip the confirmation prompt and apply changes immediately.
.TP
.B \-n
Dry run. Print the list of files that would be modified but make no changes.
.TP
.B \-b
Write a backup of each modified file with a \fI.bak\fR suffix before applying changes.
.TP
.B \-h
Print a help message and exit.
.SH EXIT STATUS
.TP
.B 0
Success.
.TP
.B 1
Invalid arguments or root is not a directory.
.TP
.B 2
No files matched the given glob and search term.
.TP
.B 3
User aborted at the confirmation prompt.
.SH EXAMPLES
Replace all occurrences of \fIOldClass\fR with \fINewClass\fR in C++ source files
under the current directory:
.PP
.RS
.nf
far . '*.cpp' 'OldClass' 'NewClass'
.fi
.RE
.PP
Restrict to a subdirectory:
.PP
.RS
.nf
far ./src '*.cpp' 'OldClass' 'NewClass'
.fi
.RE
.PP
Skip the confirmation prompt:
.PP
.RS
.nf
far -y . '*.txt' 'foo' 'bar'
.fi
.RE
.PP
Dry run -- show affected files without modifying them:
.PP
.RS
.nf
far -n . '*.h' 'TODO' 'FIXME'
.fi
.RE
.PP
Replace with backups:
.PP
.RS
.nf
far -b . '*.cpp' 'OldClass' 'NewClass'
.fi
.RE
.SH DEPENDENCIES
.TP
.BR fd (1)
Used for fast filename glob traversal.
Available at https://github.com/sharkdp/fd or via your package manager.
.TP
.BR rg (1)
ripgrep, used for fast parallel file content search.
Available at https://github.com/BurntSushi/ripgrep or via your package manager.
.TP
.BR fsed (1)
Bundled from CppSed and used for in-place substitution.
.SH CAVEATS
The
.I find
pattern is matched by
.BR rg (1)
and substituted by
.BR "fsed -E" ,
so extended regular expressions are supported and replacements may use capture
groups such as
.BR \\\\1 .
.PP
Strings containing
.B /
or
.B &
may need escaping depending on the pattern and replacement text.
.PP
For slash-heavy patterns,
.B far
also tolerates a leading delimiter slash, so inputs such as
.B /\\/\\/ --\ 
are accepted.
.PP
Without
.B \-b
there is no undo. Use
.B \-n
to preview before committing.
.SH AUTHOR
Written by Christian Schladetsch.
.SH SEE ALSO
.BR fd (1),
.BR rg (1),
.BR fsed (1),
.BR find (1)
